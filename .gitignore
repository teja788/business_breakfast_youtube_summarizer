# Keep results (telugu_transcript/english_translation/summary/kutumba_rao),
# but never commit the scratch dirs or media.
output/_audio/
output/_subs/
output/_fetch_meta.json
*.mp3
*.vtt
cookies.txt
__pycache__/
*.pyc
.venv/

# Secrets — NEVER commit (the daily automation runs `git add -A`).
.env

# Local-only: large media downloads, runtime logs, one-off backups.
data/
logs/
backup_local/
